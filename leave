--// SERVICES
local Players = game:GetService("Players")
local ProximityPromptService = game:GetService("ProximityPromptService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Workspace = game:GetService("Workspace")

local player = Players.LocalPlayer

--// GUI
local gui = Instance.new("ScreenGui")
gui.ResetOnSpawn = false
gui.Parent = player:WaitForChild("PlayerGui")

-- AUTO GRAB BUTTON
local autoBtn = Instance.new("TextButton")
autoBtn.Size = UDim2.new(0, 100, 0, 40)
autoBtn.Position = UDim2.new(1, -110, 0.3, -20)
autoBtn.BackgroundColor3 = Color3.fromRGB(200, 50, 50)
autoBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
autoBtn.Font = Enum.Font.GothamBold
autoBtn.TextSize = 18
autoBtn.Text = "Grab: OFF"
autoBtn.BorderSizePixel = 0
autoBtn.Parent = gui

-- STATE
local autoEnabled = false
local grabRange = 12
local holdingPrompts = {}
local holdStartTimes = {}
local ragdolledThisHold = {}

-- TOGGLE
autoBtn.MouseButton1Click:Connect(function()
	autoEnabled = not autoEnabled
	if autoEnabled then
		autoBtn.Text = "Grab: ON"
		autoBtn.BackgroundColor3 = Color3.fromRGB(0, 170, 0)
	else
		autoBtn.Text = "Grab: OFF"
		autoBtn.BackgroundColor3 = Color3.fromRGB(200, 50, 50)
		for prompt in pairs(holdingPrompts) do
			pcall(function() prompt:InputHoldEnd() end)
		end
		holdingPrompts = {}
		holdStartTimes = {}
		ragdolledThisHold = {}
	end
end)

-- HELPER: FIND PART
local function findAnyPart(prompt)
	if not prompt or not prompt.Parent then return nil end
	if prompt.Parent:IsA("BasePart") then return prompt.Parent end
	if prompt.Parent:IsA("Model") then
		if prompt.Parent.PrimaryPart then return prompt.Parent.PrimaryPart end
		for _,v in ipairs(prompt.Parent:GetDescendants()) do
			if v:IsA("BasePart") then return v end
		end
	end
	return nil
end

-- ADMIN PANEL COMMANDS
local function getOtherPlayer()
	for _, plr in ipairs(Players:GetPlayers()) do
		if plr ~= player then
			return plr
		end
	end
end

local function runCommand(cmd)
	local target = getOtherPlayer()
	if not target then return end
	pcall(function()
		ReplicatedStorage
			:WaitForChild("Packages")
			:WaitForChild("Net")
			:WaitForChild("RE/AdminPanelService/ExecuteCommand")
			:FireServer(target, cmd)
	end)
end

-- AP SPAM
local function runAPSpam()
	task.spawn(function()
		runCommand("tiny")
		runCommand("inverse")
		runCommand("jumpscare")
		runCommand("morph")
		runCommand("rocket")
		runCommand("balloon")
		task.wait(1.5)
		runCommand("jail")
	end)
end

-- RAGDOLL
local function doRagdoll()
	pcall(function()
		ReplicatedStorage
			:WaitForChild("Packages")
			:WaitForChild("Net")
			:WaitForChild("RE/AdminPanelService/ExecuteCommand")
			:FireServer(player, "ragdoll")
	end)
end

-- CHECK DISTANCE AND HOLD
local function tryHoldPrompt(prompt)
	if not autoEnabled then return end
	local char = player.Character
	if not char then return end
	local hrp = char:FindFirstChild("HumanoidRootPart")
	if not hrp then return end

	local part = findAnyPart(prompt)
	if not part then return end
	if (hrp.Position - part.Position).Magnitude > grabRange then
		-- out of range
		if holdingPrompts[prompt] then
			pcall(function() prompt:InputHoldEnd() end)
			holdingPrompts[prompt] = nil
			holdStartTimes[prompt] = nil
			ragdolledThisHold[prompt] = nil
		end
		return
	end

	-- in range → hold continuously
	pcall(function() prompt:InputHoldBegin() end)
	if not holdingPrompts[prompt] then
		holdingPrompts[prompt] = true
		holdStartTimes[prompt] = tick()
		ragdolledThisHold[prompt] = false
	end

	-- Ragdoll after 1.4s
	if holdStartTimes[prompt] and not ragdolledThisHold[prompt] and (tick() - holdStartTimes[prompt]) >= 1.4 then
		doRagdoll()
		ragdolledThisHold[prompt] = true
	end
end

-- CONNECT TO ALL EXISTING AND NEW PROMPTS
for _, prompt in ipairs(Workspace:GetDescendants()) do
	if prompt:IsA("ProximityPrompt") then
		task.spawn(function()
			while prompt.Parent do
				task.wait(0.05)
				tryHoldPrompt(prompt)
			end
		end)
	end
end

Workspace.DescendantAdded:Connect(function(desc)
	if desc:IsA("ProximityPrompt") then
		task.spawn(function()
			while desc.Parent do
				task.wait(0.05)
				tryHoldPrompt(desc)
			end
		end)
	end
end)

-- PROMPT TRIGGER → AP SPAM
ProximityPromptService.PromptTriggered:Connect(function(prompt, _, triggeringPlayer)
	if triggeringPlayer and triggeringPlayer ~= player then return end
	if not autoEnabled then return end
	runAPSpam()
end)
