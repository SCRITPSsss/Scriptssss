--// SERVICES
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local ProximityPromptService = game:GetService("ProximityPromptService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Workspace = game:GetService("Workspace")

local player = Players.LocalPlayer

--// GUI
local gui = Instance.new("ScreenGui")
gui.ResetOnSpawn = false
gui.Parent = player:WaitForChild("PlayerGui")

-- AUTO GRAB BUTTON (single button)
local autoBtn = Instance.new("TextButton")
autoBtn.Size = UDim2.new(0, 100, 0, 40)
autoBtn.Position = UDim2.new(1, -110, 0.3, -20)
autoBtn.BackgroundColor3 = Color3.fromRGB(200, 50, 50)
autoBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
autoBtn.Font = Enum.Font.GothamBold
autoBtn.TextSize = 18
autoBtn.Text = "Grab: OFF"
autoBtn.Parent = gui
autoBtn.BorderSizePixel = 0

autoBtn.MouseEnter:Connect(function()
	autoBtn.BackgroundColor3 = Color3.fromRGB(180, 40, 40)
end)
autoBtn.MouseLeave:Connect(function()
	if autoBtn.Text == "Grab: ON" then
		autoBtn.BackgroundColor3 = Color3.fromRGB(0, 170, 0)
	else
		autoBtn.BackgroundColor3 = Color3.fromRGB(200, 50, 50)
	end
end)

-- STATE
local autoEnabled = false
local grabRange = 12
local holdingPrompts = {} -- track client holds (true while we are holding)
local ragdollTimers = {} -- track ragdoll timers for prompts

-- Toggle
autoBtn.MouseButton1Click:Connect(function()
	autoEnabled = not autoEnabled
	if autoEnabled then
		autoBtn.Text = "Grab: ON"
		autoBtn.BackgroundColor3 = Color3.fromRGB(0, 170, 0)
	else
		autoBtn.Text = "Grab: OFF"
		autoBtn.BackgroundColor3 = Color3.fromRGB(200, 50, 50)
		-- stop all holds
		for prompt,_ in pairs(holdingPrompts) do
			pcall(function() prompt:InputHoldEnd() end)
		end
		holdingPrompts = {}
		ragdollTimers = {}
	end
end)

-- helper: find a basepart for a prompt
local function findAnyPart(prompt)
	if not prompt or not prompt.Parent then return nil end
	local parent = prompt.Parent
	if parent:IsA("BasePart") then return parent end
	if parent:IsA("Model") then
		if parent.PrimaryPart then return parent.PrimaryPart end
		for _,v in ipairs(parent:GetDescendants()) do
			if v:IsA("BasePart") then return v end
		end
	end
	local p = parent
	while p and p.Parent do
		p = p.Parent
		if p:IsA("BasePart") then return p end
	end
	return nil
end

-- AP SPAM FUNCTIONS
local function getOtherPlayer()
	for _, plr in ipairs(Players:GetPlayers()) do
		if plr ~= player then
			return plr
		end
	end
	return nil
end

local function runCommand(commandName)
	local target = getOtherPlayer()
	if not target then return end
	local args = {target, commandName}
	pcall(function()
		ReplicatedStorage
			:WaitForChild("Packages")
			:WaitForChild("Net")
			:WaitForChild("RE/AdminPanelService/ExecuteCommand")
			:FireServer(unpack(args))
	end)
end

local function runAPSpam()
	-- run sequence (non-blocking)
	task.spawn(function()
		runCommand("tiny")
		runCommand("inverse")
		runCommand("jumpscare")
		runCommand("morph")
		runCommand("rocket")
		runCommand("balloon")
		task.wait(1.5) -- jail happens after 1.5s now
		runCommand("ragdoll")
		runCommand("jail")
	end)
end

-- Ragdoll function (called after 1.4s hold)
local function doRagdoll()
	pcall(function()
		local args = {player, "ragdoll"}
		ReplicatedStorage
			:WaitForChild("Packages")
			:WaitForChild("Net")
			:WaitForChild("RE/AdminPanelService/ExecuteCommand")
			:FireServer(unpack(args))
	end)
end

-- MAIN: auto-hold prompts that are in range
RunService.RenderStepped:Connect(function()
	if not autoEnabled then return end
	local char = player.Character
	if not char then return end
	local hrp = char:FindFirstChild("HumanoidRootPart")
	if not hrp then return end

	-- iterate workspace prompts
	for _, prompt in ipairs(Workspace:GetDescendants()) do
		if prompt:IsA("ProximityPrompt") and prompt.Enabled then
			local part = findAnyPart(prompt)
			if part then
				local dist = (hrp.Position - part.Position).Magnitude
				if dist <= grabRange then
					-- ensure we call InputHoldBegin every frame while in range
					pcall(function() prompt:InputHoldBegin() end)
					holdingPrompts[prompt] = true
				else
					-- stop holding if we had been holding
					if holdingPrompts[prompt] then
						pcall(function() prompt:InputHoldEnd() end)
						holdingPrompts[prompt] = nil
						ragdollTimers[prompt] = nil
					end
				end
			end
		end
	end
end)

-- When player begins holding (exact event)
ProximityPromptService.PromptButtonHoldBegan:Connect(function(prompt, inputType)
	if not autoEnabled then return end
	-- start timer entry; store start time
	ragdollTimers[prompt] = tick()
	-- schedule ragdoll check after 1.4s
	task.delay(1.4, function()
		if ragdollTimers[prompt] and holdingPrompts[prompt] and autoEnabled then
			doRagdoll()
			ragdollTimers[prompt] = nil
		end
	end)
end)

-- When player stops holding
ProximityPromptService.PromptButtonHoldEnded:Connect(function(prompt, inputType)
	ragdollTimers[prompt] = nil
end)

-- WHEN PROMPT FINISHES (triggered) run AP spam immediately
ProximityPromptService.PromptTriggered:Connect(function(prompt, inputType, triggeringPlayer)
	if triggeringPlayer and triggeringPlayer ~= player then return end
	if not autoEnabled then return end
	runAPSpam()
end)
